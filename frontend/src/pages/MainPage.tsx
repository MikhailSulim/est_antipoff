import React from 'react';
import Header from '../components/Header/Header';

const MainPage = () => {
  return (
    <div>
      <Header main={true} />
      MainPage
    </div>
  );
};

export default MainPage;
